```
Boolean canChange = false

Calendar calendar = Calendar.getInstance()  
calendar.setTime(lastUpdated)  
calendar.set(Calendar.MONTH, calendar.get(Calendar.MONTH) + 3)  
if(calendar.getTime() < new Date()) canChange = true
```


