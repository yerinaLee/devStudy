docker 시작

```
docker compose up -d
```


docker 종료
```
docker compose down
```


- `docker compose stop` → 컨테이너만 일시정지 (나중에 `start`로 재시작 가능)
- `docker compose down` → 컨테이너 + 네트워크 삭제 (완전히 내림)
- `docker compose down -v` → 볼륨까지 삭제 (DB 데이터 날아갈 수 있음)



