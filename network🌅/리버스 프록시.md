## 리버스 프록시가 하는 일

```
[현재]
외부 → 포트 8080 (HTTP, 평문) → Spring Boot

[리버스 프록시 추가 후]
외부 → 포트 443 (HTTPS, 암호화) → Nginx → 포트 8080 (내부, HTTP) → Spring Boot
```

Nginx(리버스 프록시)가 중간에서:

1. HTTPS 암호화/복호화 처리 (Spring Boot는 신경 안 써도 됨)
2. 포트 8080은 외부에 완전히 닫힘 → 443만 열림
3. 요청이 Spring Boot 도달 전에 1차 필터링 가능